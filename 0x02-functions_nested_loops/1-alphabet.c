#include "main.h"

/**
 * Print-alphabet - Print the alphabet in lowercase
 * Description: Just anything\
 * Return: Always 0
 */
void print_alphabet(void)
{
	char ls;

	for (ls = 'a'; ls <= 'z'; ls++)
		_putchar(ls);

	_putchar('\n');
}
